import "./NavBar.css";

const NavBar = () => {
  return (
    <nav className="nav-menu">
      <ul className="options">
        <li className="option">
          <span className="text">Details</span>
        </li>
      </ul>
    </nav>
  );
};

export default NavBar;
